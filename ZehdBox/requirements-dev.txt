# Dépendances de développement du site ZehdBox
# Installer avec : pip install -r requirements-dev.txt
#
# Cette liste ne contient que les dépendances réellement utilisées par le
# projet. Elle remplace l'ancien `pip freeze` de la machine complète, qui
# embarquait des paquets système Ubuntu non installables via pip.

# --- Dépendances du site (identiques à la CI) ---
-r requirements-ci.txt

# --- Outils de développement optionnels ---

# Diff de relecture et blocs de code : fournis par Material, rien à ajouter.

# nox / pytest non utilisés : le projet n'a pas de tests automatisés.

# --- Dépendances de la branche feature (exercices interactifs) ---
# Décommenter uniquement pour travailler sur feature/exercices-python-interactifs.
# pyodide-mkdocs-theme==5.4.16
